import { HighlightAlterDirective } from './highlight-alter.directive';

describe('HighlightAlterDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightAlterDirective();
    expect(directive).toBeTruthy();
  });
});
