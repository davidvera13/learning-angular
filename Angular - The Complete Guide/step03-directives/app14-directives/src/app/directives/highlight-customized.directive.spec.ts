import { HighlightCustomizedDirective } from './highlight-customized.directive';

describe('HighlightCustomizedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCustomizedDirective();
    expect(directive).toBeTruthy();
  });
});
